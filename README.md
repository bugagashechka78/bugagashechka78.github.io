Чтобы запустить новый проект с Ionic нужно:

```npm install```

для запуска в браузере: 

```ionic serve```

для запуска в эмуляторе:

```ionic build```

```npx cap copy & npx cap sync & npx cap open android```
